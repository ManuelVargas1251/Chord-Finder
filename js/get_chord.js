function get_chord(root, intervals){

	let chord = ""
	let message = "Interval Count: "

	switch (intervals.length) {
		case 1:
			console.log(message + intervals.length)
			break;	
		case 2:
			console.log(message + intervals.length)
			break;
		case 3:

			console.log(message + intervals.length)

			_intervals.getInt()
			
			break;
		default:


	}





	return root + chord
}